ALTER TABLE card
    ADD COLUMN IF NOT EXISTS oracle_text TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS power TEXT,
    ADD COLUMN IF NOT EXISTS toughness TEXT,
    ADD COLUMN IF NOT EXISTS loyalty TEXT,
    ADD COLUMN IF NOT EXISTS defense TEXT,
    ADD COLUMN IF NOT EXISTS keywords TEXT NOT NULL DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS produced_mana TEXT NOT NULL DEFAULT '[]',
    ADD COLUMN IF NOT EXISTS rarity TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS collector_number TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS set_name TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS artist TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS flavor_text TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS legalities TEXT NOT NULL DEFAULT 'null',
    ADD COLUMN IF NOT EXISTS edhrec_rank INTEGER,
    ADD COLUMN IF NOT EXISTS penny_rank INTEGER,
    ADD COLUMN IF NOT EXISTS oracle_id TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS scryfall_id TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS scryfall_uri TEXT NOT NULL DEFAULT '';

CREATE INDEX IF NOT EXISTS card_rarity_idx ON card (rarity);
